deployments:
  - hextech-common:
    container_image: common
    targets:
      - module.common_ecs_alb
  - hextech-common-lambda:
    container_image: common-lambda
    targets:
      - module.common_api
      - module.common_tasks
