# Copy this to .env and fill in your values

# Your Anthropic API key — setting this makes the Claude Code CLI (and therefore
# the agent SDK) use direct API billing instead of your Claude Code subscription.
ANTHROPIC_API_KEY=your_api_key_here
