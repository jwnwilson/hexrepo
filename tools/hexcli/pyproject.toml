[tool.poetry]
name = "hexcli"
version = "0.1.0"
description = ""
authors = ["Noel Wilson <jwnwilson@hotmail.co.uk>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
typer = "^0.15.1"
cookiecutter = "^2.6.0"
pyyaml = "^6.0.2"
boto3 = "^1.35.58"
pydantic-settings = "^2.6.1"

[tool.poetry.scripts]
hexcli = "hexcli.main:app"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
